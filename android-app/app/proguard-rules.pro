# Intentionally empty: add shrinking rules when external SDKs are introduced.
# Retrofit service interfaces are resolved reflectively by Retrofit.
-keepattributes Signature, InnerClasses, EnclosingMethod
